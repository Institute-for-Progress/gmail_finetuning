# Model Configuration Options

# GPT-3.5 Turbo Configuration
# MODEL_NAME=gpt-3.5-turbo
# MODEL_SUFFIX=email_tuned
# MAX_TOKENS=300
# TEMPERATURE=0.7  # Supports temperature range 0.0-2.0

# GPT-4 Turbo Configuration
# MODEL_NAME=gpt-4-turbo-preview
# MODEL_SUFFIX=email_tuned
# MAX_TOKENS=300
# TEMPERATURE=0.7  # Supports temperature range 0.0-2.0

# GPT-4.1 Configuration (Default)
MODEL_NAME=gpt-4.1-2025-04-14
MODEL_SUFFIX=email_tuned
MAX_TOKENS=300
TEMPERATURE=0.7  # Supports temperature range 0.0-2.0

# Training Configuration
MONITORING_INTERVAL=60

# File Paths
TAKEOUT_DIR=data/Takeout
TRAINING_FILE=training_data.jsonl

# System Configuration
SYSTEM_PROMPT="You are an email assistant. Your task is to generate professional email responses. When given an email, you should:
1. Maintain a tone that is by default professional, or appropriately mirrors the message you're replying to
2. Address all points in the original email
3. Keep responses concise but complete
4. Include appropriate greeting and closing when the email is relatively formal
5. Format the response as a proper email"

# Fine-tuning IDs
# Get these from your OpenAI dashboard after training:
# 1. Go to https://platform.openai.com/finetune
# 2. Find your fine-tuned model
# 3. Copy the model ID (it will look like 'ft:gpt-4.1-2025-04-14:your-org::your-model-id')
TRAINING_FILE_ID=
JOB_ID=
TRAINED_MODEL_ID=  # Paste your fine-tuned model ID here

# Your email address
YOUR_EMAIL_ADDRESS=yourname@email.com
